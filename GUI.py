from tkinter import *
from tkinter import ttk

window = Tk()
window.geometry("420x420")
window.title("Automated Archival Process")

label = Label(window, text="Data Accessioner", font=("Arial", 15, "bold"))
label.pack()

window.mainloop()
